%% Layer Hierarchy — Floor
%% Arrows show permitted reference direction (downward only).
%% Generated by architecture health check. Do not edit manually.

graph TB
    subgraph L0["Layer 0 — Entry"]
        floor["floor.py"]
    end

    subgraph L1["Layer 1 — Orchestrator"]
        shutdown["shutdown.py"]
    end

    subgraph L2["Layer 2 — Check"]
        check_manifest["check_manifest.py"]
        check_cdocs["check_cdocs.py"]
        check_coverage["check_cdoc_coverage.py"]
    end

    subgraph L3["Layer 3 — Domain"]
        task_counter["task_counter.py"]
    end

    subgraph L4["Layer 4 — Utility"]
        hash_util["hash_util.py"]
    end

    subgraph docs["Project Management (read/written by scripts and Claude)"]
        prompting["prompting.md"]
        status["status.md"]
        manifest["manifest.md"]
        cdocs["cdocs/*.md"]
        implement["prompts/implement-this.md"]
        standards["standards/*.md"]
        cdoc_instr["cdoc.md"]
    end

    L0 -->|"subprocess"| L1
    L0 -->|"subprocess"| L2
    L0 -->|"subprocess"| L3
    L1 -->|"subprocess"| L2
    L1 -->|"subprocess"| L3
    L2 -->|"import"| L4

    L0 -.->|"reads"| docs
    L2 -.->|"reads"| docs
